# Loose development dependency lower bounds (human-readable).
# For reproducible installs, prefer the locked list instead:
#   python -m pip install -r requirements-dev.lock
#
# Regenerate the lockfile with:
#   pip-compile pyproject.toml --extra dev -o requirements-dev.lock

pytest>=8
pytest-cov>=5
ruff>=0.8
mypy>=1.13
